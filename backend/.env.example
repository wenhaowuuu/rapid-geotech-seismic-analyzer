# Allowed CORS origins (comma-separated). Set to your GitHub Pages URL in prod.
ALLOWED_ORIGINS=*

# Geocoding toggles
USE_CENSUS_GEOCODER=true
USE_NOMINATIM_FALLBACK=true

# ArcGIS layer endpoints (set real CGS URLs, e.g. .../FeatureServer/0)
CGS_AP_FAULT_ZONE_URL=
CGS_LIQUEFACTION_ZONE_URL=
CGS_LANDSLIDE_ZONE_URL=
